Role_app:
  vars:
    name: app-server
    newvar1: The thing
    newvar2:
      - one
      - two
      - three
Role_db:
  vars:
    name: db-server
Role_web:
  vars:
    name: web-server
    port: 80
me:
  hosts:
    rick:
