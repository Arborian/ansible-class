Role_app:
  vars:
    name: app-server
    newvar1: The thing
    newvar2:
      - one
      - two
      - three
Role_db:
  vars:
    name: db-server
Role_web:
  vars:
    name: web-server
    port: 80
me:
  hosts:
    rick:
  vars:
    roster:
    - name: Rick Copeland
      email: rick@arborian.com
      regexp: rick
    - name: Marc Benioff
      email: marc@salesforce.com
      regexp: marc
    - name: Patrick P. Gelsinger
      email: pat@vmware.com
      regexp: pat
