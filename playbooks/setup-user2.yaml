- hosts: me
  become: yes
  vars_prompt:
    - name: username
      prompt: What is your username?
  tasks:
  - name: create user
    user:
        name: '{{username}}'
        groups: wheel
        generate_ssh_key: yes
        shell: /bin/bash
